# AIエージェント / 競輪AI 開発アシスタント

このリポジトリで作業を再開するときは、最初に `AI_AGENT_REQUIREMENTS.md`、`WORK_STATUS.md`、`main` の現行コードを確認する。古い引き継ぎ文より現物を優先する。ユーザーが「再開」「続けて」と指示した場合は、安全条件を維持したまま次の未完了作業へ進む。

最終目標は競輪AI専用の開発代行ではない。情報収集、文章・画像・動画・プログラム作成、学習、タスク実行、検証、復旧、報告まで扱う広い用途の自律型AIエージェントを作る。競輪AIは最初の重要な実行対象であり、既存要件を削除・置換せずに共通エージェント基盤へ載せる。

## 作業手順

1. `AI_AGENT_REQUIREMENTS.md` の全体目標と固定条件を確認する。
2. `main` の最新状態、関係ファイル、必要なら配備済み関数の契約を確認する。
3. 変更は小さいブランチ / PR に分け、対象に合う回帰テストを追加または実行する。
4. CI が成功し、変更内容を確認できた場合だけ `main` へ反映する。
5. `WORK_STATUS.md` の確認済み進捗・未確認事項・次の作業を現行状態に合わせる。
6. 検証していない動作を成功と報告しない。権限・404・CI失敗などは同じ操作を反復せず、原因を切り分ける。
7. 作成・検証・永続保存・ユーザー端末保存・画面読込を別々に判定し、所在不明と未作成を混同しない。
8. 完了済み工程を再実行する前に既存成果物と完了証拠を確認する。

## 共通エージェント方針

- スマホから指示し、クラウド側でGitHub、Supabase、Web、ファイル、生成AIなどを工程に応じて使い分ける。
- 同じ説明、スクリーンショット収集、JSON受け渡し、手動再読込を可能な範囲で減らす。
- 本人操作が不可欠な場合だけ、1画面・1操作単位で案内する。
- 実行環境、Task model、永続状態、Tool router、Runner、Verifier、Recovery、Activity ledger、Reportingを共通基盤として実装する。
- 利用できない接続や機能を利用済みとして扱わない。
- 必要な権限、契約、課金、認証を無制限に承認されたものと扱わない。
- 21時報告は製品要件として維持するが、売上データ源・計上基準・配信先が未確定の間は未取得値を推測や0埋めしない。

## 維持する競輪AI安全条件

- 本番AI予想 OFF。
- 開発フローからの DB 書き込み OFF。
- 外部データ自動取得 OFF。
- owner 専用 dry-run を使用し、再現・旧データと前向き検証を区別する。
- 未確認オッズを補完しない。予測時点で確認できたオッズだけを保持する。
- 確率は未校正として扱い、金銭的 EV や本番昇格判断に使用しない。
- 秘密鍵、認証トークン、個人情報、非公開資料を公開リポジトリへ保存しない。
- 実レースの予測スナップショット、履歴、モデル成果物、評価レポートは非公開のまま扱う。
- 認証やアクセス制御を回避しない。

これらを変更する場合は、ユーザーの明確な指示と、その変更に必要な安全確認を行う。

## 現行構成

- `index.html` — owner 向け公開 Web。実レース dry-run、結果確定→履歴生成、複数履歴統合、バックテスト表示。
- `prospective-tools.html` / `prospective-tools-core.js` / `prospective-tools-ui.js` — 外部通信なしのブラウザ内前向き検証ツール。スナップショットの履歴化と収集状況確認。
- `ml/` — 学習データ構築、LightGBM 位置モデル、推論、時系列の paired offline evaluation、収集状況確認、オフライン settlement。
- `supabase/functions/` — 配備対象の Edge Function ソースとデータ契約。
- `tests/` — Web、安全性、ML、時系列、契約テスト。
- `.github/workflows/` — CI と GitHub Pages 関係。

共通エージェント基盤は未完成。追加実装は `AI_AGENT_REQUIREMENTS.md` の優先順位に従う。

## 前向き実データ

前向き精度比較に使う実データは、発走前に保存された不変スナップショットと、結果確定後に作った適格履歴だけを使用する。結果を見た後に予測時点の入力を変更しない。

保存済みの予定発走時刻も因果順序の一部として扱う。スナップショット保存は予定発走より前、結果確認時刻は予定発走以降でなければならない。スナップショット側とレース入力側の予定時刻が両方ある場合は、同一時点を表していることを確認する。

`ml.evaluate_offline` の 5 つの異なる適格予測時刻は、時系列分割を試せる技術上の最低条件にすぎない。境界 purge により 5 件を超えて必要になる場合があり、5 件到達は統計的十分性・モデル優位性・収益性を示さない。

詳しい収集手順は `PROSPECTIVE_COLLECTION.md` を参照する。

## 再開時の優先順位

1. `AI_AGENT_REQUIREMENTS.md` と `WORK_STATUS.md` を確認する。
2. 現行 `main` と CI が記録と一致しているか確認する。
3. 次工程が共通AIエージェントの全体目標に寄与するか確認する。
4. ユーザー操作を必要としない安全な開発・テスト・ドキュメント整理を先に進める。
5. 競輪データ収集だけを続けて共通エージェント基盤を置き去りにしない。
6. 実レース入力、認証済みブラウザ操作、ユーザー端末上の私有ファイルなど本人操作が必要な段階だけ、必要な1操作を具体的に案内する。
