# 開発状態

更新日: 2026-09-23。再開時は現行コードと配備状態を再確認する。

## 完了

- Webコードの棚卸し。基準コミット: 7dd612042242a58613084bb12e356d748f8eaa15。
- index.html に現行エンジン判定と「結果確定 → 履歴JSON生成」画面が存在することを確認。
- GitHub Appの再インストール後、ブランチ作成とファイル追加に成功。以前の403は今回の操作で解消。
- AGENTS.md に作業再開、検証、引き継ぎの手順を追加。

## 現在の範囲

Work内で作業するアシスタントの引き継ぎ基盤。独立したAI実行サービス、常駐監視、音声応答は未実装。
この変更は文書のみ。Web実装とバックエンドを変更していない。

## 次の作業

1. 保存済みスナップショットと対応する確定結果を照合し、履歴生成からバックテストまでの保存なし検証を実施する。過去レースの結果や対応ファイルを推測で補わない。
2. 公開画面とowner認証後の操作を確認する。現時点ではコードの存在を確認したのみ。
3. 開発用バックエンドソースのバージョン管理と、再現可能なテストを整備する。
4. 実際の反復作業を自動化し、独立アシスタントに必要な実行環境・認証・非公開記憶・費用を決める。

## 固定条件

予想DB書き込みOFF / 本番予想OFF / 外部データ自動取得OFF。
事前保存のない過去レースは再現検証とし、前向き実績に含めない。
