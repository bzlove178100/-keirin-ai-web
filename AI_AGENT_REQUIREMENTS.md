# AIエージェントと競輪AI：共通開発要件

最終更新: 2026-09-24

この文書は、競輪AIの既存要件を維持しつつ、開発全体の最終目標を「広い用途の自律型AIエージェント」として固定するための共通要件である。競輪AIは最初の重要な実行対象だが、AIエージェントの用途を競輪AI専用へ縮小しない。

## 最終目標

スマホから指示でき、クラウド側で必要なツールを使い分けながら、次の仕事を自律的に進められるAIエージェントを作る。

- 情報収集
- 文章・テキスト作成
- 画像生成
- 動画生成
- プログラム作成・修正・テスト
- 学習・評価
- タスク実行と進捗管理
- 障害検知、原因切り分け、復旧
- 実行結果の記録と報告

利用可能なAI・ツールは工程ごとに組み合わせる。利用できない接続や機能を利用済みとして扱わない。必要な権限、認証、契約、課金を無制限に承認されたものと解釈しない。

## 競輪AIとの関係

競輪AIの既存条件は削除・置換しない。共通エージェント基盤は、競輪AIの開発、検証、データ整理、テスト、Web連携、将来の安全な自動化を効率化する最初の実運用対象として使う。

現時点では次を維持する。

- 本番AI予想 OFF
- 開発フローからのDB書き込み OFF
- 外部データ自動取得 OFF
- owner専用dry-runを使用
- 再現・旧データと前向き検証を区別
- 未確認オッズを補完しない
- 未校正確率を金銭的EVや本番昇格判断に使用しない
- 実レースの私有スナップショット、履歴、モデル成果物、評価レポートを公開リポジトリへ保存しない

これらを有効化・変更する場合は別途の明確な指示と安全確認を必要とする。

## スマホ運用と手作業削減

ユーザーのスマホ操作負担を減らす。

- 同じ説明を繰り返させない
- 既存成果物を確認せず再作成させない
- 手動スクリーンショット収集を減らす
- JSONの手渡し・再読込を減らす
- 本人操作が不可欠な場合だけ、1画面・1操作単位で案内する
- 所在不明と未作成を混同しない
- 作成、検証、永続保存、端末保存、画面読込を別々に判定する

## 自律稼働に必要な共通基盤

実装対象は次の通り。

1. **Task model** — 仕事の目的、入力、許可された操作、完了条件、検証条件、失敗時の扱いを機械可読に管理する。
2. **Persistent state** — タスク状態、成果物ID、検証結果、未完了理由、次の作業を永続化する。
3. **Tool router** — GitHub、Supabase、Web、ファイル、生成AIなどを用途ごとに選択し、必要以上の権限を使わない。
4. **Execution runner** — 複数ステップの作業を順番に実行し、再試行・中断・再開を扱う。
5. **Verifier** — 変更後のテスト、契約、配備状態、保存状態を確認し、未検証を完了扱いしない。
6. **Recovery** — CI失敗、権限不足、接続断、入力不足を分類し、同じ失敗操作の無限反復を防ぐ。
7. **Activity ledger** — 何を実行し、何が成功し、何が失敗し、次に何をするかを記録する。
8. **Reporting** — 定期報告を組み立て、取得できない値を推測や0埋めで処理しない。

## 毎日21時の必須報告

日本時間 Asia/Tokyo を基準とし、最終的に毎日21:00に次を報告できるようにする。

- 当日の売上
- 当月の売上
- 当日の活動報告
  - 実行した仕事
  - 成果
  - 失敗・未完了
  - 次の作業

未確定項目は、売上の対象事業、データ源、計上基準、月間集計の定義、配信先。未取得の売上を0円と表示しない。競輪の払戻・収支を売上と同一視しない。

この文書への記載だけでは21時の自動配信・スケジュール実行が設定済みであることを意味しない。

## 再開時の必須照合

1. この文書を読む。
2. `AGENTS.md`、`WORK_STATUS.md`、`main`、必要な配備状態を照合する。
3. 記録と現物が食い違う場合は現物を優先し、差を明示する。
4. 完了済み工程を再実行する前に、成果物と完了証拠を確認する。
5. 次工程がAIエージェント全体目標に寄与するか確認する。
6. 競輪データ収集だけを続けて共通エージェント基盤を置き去りにしない。
7. 検証済み成果、成果物識別子、残る問題、次の1手を残す。
8. 「絶対に忘れない」「ミスがゼロ」と保証せず、記録、現物照合、検証、重複防止で再発を減らす。

## 2026-09-24時点の確認済み基準

- 伊東温泉1Rの発走前スナップショットと、結果確定後の適格履歴が私有領域に保存済み。
- その履歴は7選手、Phase32の全210通りの確率、予測時点で既知だった72通りの3連単オッズを保持し、前向き検証として適格確認済み。
- 実データ収集は異なる適格予測時刻1件。最低5時点まであと4時点。ただし5時点は技術上の最低条件であり、精度、モデル優位性、収益性を示さない。
- LightGBMオフライン推論、時系列paired evaluation、履歴生成、収集進捗表示、厳密な前向き準備判定はGitHub側で実装済み。

## 次に優先する開発

競輪AIの条件を保持したまま、共通エージェント基盤と手作業削減を進める。

優先順は次の通り。

1. 共通Task modelと永続状態の最小実装
2. GitHub作業を対象にした実行runnerとverification loop
3. 競輪AI開発タスクを共通Task modelへ載せるアダプタ
4. activity ledgerと再開時の重複防止
5. 21時報告のデータ契約を作り、売上データ源と配信先が決まった後に定期実行へ接続
6. 画像・動画・文章・一般プログラム作業へツールルーティングを拡張

レース資料の送付依頼へ自動的に戻らない。本人操作が必要になるまで、安全なコード・テスト・ドキュメント作業を先に進める。
